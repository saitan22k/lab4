import java.util.Scanner;

public class Main {
    public static void main(String[] args)
    {

        Scanner scan = new Scanner(System.in);
        Magaz magaz=new Magaz();
        int count = scan.nextInt();
        scan.nextLine();

        Phone[] phones = magaz.infPhones(count);
        magaz.getPhones(phones);
    }
}
